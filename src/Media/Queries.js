const wideScreen = 1440;
const desktopWide = 1024;
const tabletWide = 768;
const mobileWide = 480;

export const widescreen = `@media (max-width:${wideScreen}px)`;
export const desktop = `@media (max-width:${desktopWide}px)`;
export const tablet = `@media (max-width:${tabletWide}px)`;
export const mobile = `@media (max-width:${mobileWide}px)`;
